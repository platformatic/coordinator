# Supply-chain cooldown: skip dependency versions younger than ~24h.
# Filters out smash-and-grab incidents that get yanked within hours.
# npm CLI 11.10+ uses days; pnpm 10.x uses minutes. Set both for cross-tool safety.
min-release-age=1
minimum-release-age=1440
